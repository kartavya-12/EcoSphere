const { createClient } = require("@supabase/supabase-js"); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.auth.signUp({ email: "testrls55@example.com", password: "password123" }); if (error) { console.log("Signup error:", error.message); return; } const user = data.user; console.log("User signed up:", user?.id); const newProfile = { id: user.id, name: "Test User", role: "community", trees_planted: 0, cleanup_drives: 0, rank: "Eco-Warrior" }; const { error: insertErr } = await supabase.from("profiles").insert([newProfile]); console.log("Profile insert error:", insertErr); } test();
